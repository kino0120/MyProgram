
// launch.json
{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "gdb",
            "request": "launch",
            "name": "GDB: Launch Program",
            "target": "main.exe",
            "cwd": "${workspaceRoot}",
            //"terminal": "",  // コンソールウィンドウを表示したい場合は追加
            "externalConsole": true, // ここをtrueに変更する
            "preLaunchTask": "build / debug file"
        }
    ]
}
